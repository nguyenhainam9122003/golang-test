# Where are all the schema files located?
schema:
  - graphql/*.graphqls

# Where should the generated server code go?
exec:
  filename: graphql/generated.go
  package: graphql

# Where should any generated models go?
model:
  filename: models/generated.go
  package: models

# Where should the resolver implementations go?
resolver:
  layout: follow-schema
  dir: graphql
  package: graphql
  filename_template: "{name}.resolvers.go"

# Optional: turn on use ` + "`" + `gqlgen:"fieldName"` + "`" + ` tags in your models
# struct_tag: json

# Optional: turn on to use []Thing instead of []*Thing
# omit_slice_element_pointers: false

# Optional: turn on to exclude Is<Name>() methods for interface types
# omit_interface_checks: false

# Optional: turn on to exclude the gqlgen generate version header
# omit_gqlgen_version: false

# Optional: turn on to exclude the complexity limit from type generation
# omit_complexity: false

# Optional: turn on to not generate any file notice headers
# omit_generated_notice: false

# gqlgen will search for any type names in the schema in these go packages
# if they match it will use them, otherwise it will generate them.
autobind:
  - "test/graphql_service/models"

# This section declares type mapping between the GraphQL and go type systems
#
# The first line in each type will be used as defaults for resolver arguments and
# modelgen, the second line will be used when the name is a resolver input type.
#
# If you want to use a different type for your GraphQL type, you can override it here.
models:
  ID:
    model:
      - github.com/99designs/gqlgen/graphql.ID
      - github.com/99designs/gqlgen/graphql.Int
      - github.com/99designs/gqlgen/graphql.Int64
      - github.com/99designs/gqlgen/graphql.Int32
  Int:
    model:
      - github.com/99designs/gqlgen/graphql.Int
      - github.com/99designs/gqlgen/graphql.Int64
      - github.com/99designs/gqlgen/graphql.Int32 